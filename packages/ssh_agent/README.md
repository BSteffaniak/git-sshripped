# git\_sshripped\_ssh\_agent

SSH agent integration for git-sshripped. Provides native SSH agent
communication for wrapping and unwrapping repo keys without needing the
private key file on disk or a passphrase.
